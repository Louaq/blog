import type { WalineInitOptions } from '@waline/client';
export interface WalineOptions extends WalineInitOptions {
    selector?: string;
}
export declare const useWaline: (walineOptions: WalineOptions) => void;
export default useWaline;
